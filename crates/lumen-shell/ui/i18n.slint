export global I18n {
    in property <string> app-subtitle;
    in property <string> start;
    in property <string> stop;
    in property <string> status-label;
    in property <string> config-label;
    in property <string> log-label;
    in property <string> browse;
    in property <string> back;
    in property <string> next;
    in property <string> create-setup;
    in property <string> cache-label;
    in property <string> config-field-label;
    in property <string> bootstrap-label;
    in property <string> advanced-settings-label;
    in property <string> advanced-settings-hint;
    in property <string> region-other-title;
    in property <string> region-other-detail;
    in property <string> region-cn-title;
    in property <string> region-cn-detail;
    in property <string> preset-0-title;
    in property <string> preset-0-detail;
    in property <string> preset-1-title;
    in property <string> preset-1-detail;
    in property <string> preset-2-title;
    in property <string> preset-2-detail;
    in property <string> conflict-overwrite-title;
    in property <string> conflict-overwrite-detail;
    in property <string> conflict-next-title;
    in property <string> conflict-next-detail;
    in property <string> conflict-cancel-title;
    in property <string> conflict-cancel-detail;
}
