# third_party

Place the vendored ncnn checkout here:

```text
crates/lumnn-ncnn-sys/third_party/ncnn
```

For development, setting `NCNN_SOURCE_DIR=/absolute/path/to/ncnn` is equivalent
and avoids copying or submoduling the source tree while iterating.
